/**
 * Supabase больше не используется.
 * Данные хранятся в локальной SQLite-базе на сервере (папка server/).
 * Запуск: cd server && pip install -r requirements.txt && python seed.py && python app.py
 */
window.SLENG_SUPABASE = null;
